apiVersion: notification.kubesphere.io/v1alpha1
kind: SlackReceiver
metadata:
  name: admin-slack
  labels:
    type: tenant
    user: admin
spec:
  slackConfigSelector:
    matchLabels:
      type: tenant
      user: admin
  channel: slack_channel_x
