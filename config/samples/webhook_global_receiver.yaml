apiVersion: notification.kubesphere.io/v2alpha1
kind: WebhookReceiver
metadata:
  name: admin-webhook
  labels:
    type: global
spec:
  webhookConfigSelector:
    matchLabels:
      type: default
  url: xxx
