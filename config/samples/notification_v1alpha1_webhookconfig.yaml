apiVersion: notification.kubesphere.io/v1alpha1
kind: WebhookConfig
metadata:
  name: global-webhook-config
  labels:
    type: global
spec:
  url: xxx
  urlSecret:
    key: webhook-url
    name: webhook-url-secret
  httpConfigSelector:
    matchLabels:
      type: tenant
      user: admin
