apiVersion: notification.kubesphere.io/v1alpha1
kind: WebhookReceiver
metadata:
  name: admin-webhook
  labels:
    scope: tenant
    user: admin
spec:
  webhookConfigSelector:
    matchLabels:
      scope: tenant
      user: admin
